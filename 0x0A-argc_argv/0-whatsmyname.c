#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry Point
 *
 * @argc: number of argument
 * @argv: array of arguments
 *
 * Return: Alwys is 0
 */

int main(int __attribute__((unused)) argc, char* argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
